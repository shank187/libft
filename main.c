#include "libft.h"

int main(void)
{
    // printf("%s\n",ft_split("lorem ipsum dolor sit amet", 400, 20));
    // char *str = (char *)ft_calloc(30, 1);
    // char *str1 = (char *)calloc(30, 1);
    // int i = 0;
    // while(i < 30)
    // {
    //     printf("%i",str1[i]);
    //     i++;
    // }
    // char dest[15];
    // char *p = "hello";
    // char *c = "helll";
    // printf("%i\n",ft_strncmp(p, c,5));
    // printf("%i\n",strncmp(p, c,5));
    // printf("%i\n",ft_strncmp("test\200", "test\0",6));
    // printf("%i\n",strncmp("test\200", "test\0",6));
    // write(1,dest,15);
    // write(1,"\n",1);
    // printf("%lu\n",ft_strlcpy(NULL, "lorem ipsum dolor sit amet",10));
    // write(1,dest,15);
    // write(1,"\n",1);
    // printf("%lu\n",strlcpy(dest, "lorem ipsum dolor sit amet", 0));
    // write(1,dest,15);
    // write(1,"\n",1);
  	// int size = 8539;

	// void * d1 = ft_calloc(size, sizeof(int));
	// void * d2 = calloc(size, sizeof(int));
	// if (memcmp(d1, d2, size * sizeof(int)))
  //   	printf("failed");
	// free(d1);
	// free(d2);
	// printf("success");
  // int i;

 	// i = -122;
 	// while (i < 530)
 	// {
 	// 	if (ft_isprint(i) != isprint(i))
  //     printf("tits failed in %i\n",i);
  //     i++;
 	// }
 	// printf("TEST_SUCCESS\n");

	// char *src = "/|\x12\xff\x09\0\x42\042\0\42|\\";
	// int size = 12;

	// if (memchr(src, '\0', size) == ft_memchr(src, '\0', size))
  // {
  //   printf("TEST_SUCCESS\n");
  //   return 0;
  // }
  // printf("tits failed ");

  // char *s1 = "";
	// char *s2 = NULL;
	// size_t max = strlen(s2);
	// char *i1 = strnstr(s1, s2, 0);
	// char *i2 = ft_strnstr(s1, s2, 0);

	// if (i1 == i2)
	// 	printf("TEST_SUCCESS\n");
  // else
	// 	printf("TEST_FAILED\n");
  // printf("%i",ft_atoi("\t\n\r\v\f  469 \n"));
//   char **tabstr;
//   int i;
//   i = 0;
//     if (!(tabstr = ft_split("lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed non risus. Suspendisse", ' ')))
//         printf("NULL\n");
//     else
//     {
//         while (tabstr[i] != NULL)
//         {
//             printf("%s\n",tabstr[i]);
//             i++;
//         }
//     }

 	// char *s = "      split       this for   me  !       ";

 	// char **result = ft_split(s, ' ');
 	// if (!result)
 	// 	printf("TEST_SUCCESS");
  // else
 	//   printf("TEST_FAILED");

  printf("|%s|\n",ft_strtrim("",""));

}